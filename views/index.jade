extends layout

block content
    form(action="/dashboard" method="post")
        label(for="remote") remote server
            input(type="text" name="remote")
        label(for="accessLogs") access logs directory
            input(type="text" name="accessLogs")
        label(for="userName") User Name:
            input(type="text" name="userName")
        input(type="submit")
